# Changelog

版本号格式：语义化版本 `主版本.次版本.修订号`，每个版本对应一个可安装的 `.tpk`。

## v1.1.5 — 2026-09-20

### 变更

- 配置页界面统一到与 FanCtrl / 灯控中心一致的设计语言：深 / 浅色主题、页脚署名「西了个瓜」
- 精简应用描述与安装参数文案

## v1.1.4 — 2026-09-20

### 新增

- **内置配置页**：应用中心详情页点「打开」即是配置页，挂载目录、文件属主、WebDAV 地址与口令都能改，保存后自动重新挂载，不用卸载重装
- 配置持久化到 `/volume1/data/cloudmount/cloudmount.conf`，应用升级 / 覆盖安装都不会冲掉
- 配置页访问闸门：PBKDF2-HMAC-SHA256（20 万轮）口令 + 无状态签名 Cookie，首次打开由使用者自行设置

### 说明

- 做配置页的原因：应用中心对已安装的应用只给「打开 / 停用 / 卸载」，没有「设置」入口（`isShowSettings` 由云端元数据计算，自装应用恒为 false），而安装参数装完就改不了 —— 想改只能卸载重装，重装又会把参数重置回出厂默认值。于是把配置放到「打开」按钮后面。

## v1.1.3 — 2026-09-20

### 修复

- **缓存目录自绑定导致宿主挂载表被污染**：v1.1.1 / v1.1.2 把缓存的容器内目标写成了与宿主相同的路径（源 == 目标），这种自绑定会在宿主命名空间里泄露一个与存储池同设备（`/dev/dm-*`）的重复挂载点，铁牛据此把该存储池的 `MountPoint` 认成缓存目录 —— 后果是文件管理器「我的文件」根列表变空（接口 `/filemanage/file?path=/` 返回空数组），看起来像「文件夹全丢了」，而数据一个没少
- 新增 `purge_stray_cache_mount()`：升级时自动清理已经中招的机器的多余挂载点，无需手工干预

### 变更

- 缓存目录的容器内路径由同路径改为 `/cmcache`

## v1.1.2 — 2026-09-19

### 修复

- 首次安装会凭空创建一个空的默认挂载目录（`/volume1/data/网盘`）。原因是安装脚本在「挂载点尚未解析出来」时退回默认值去 `mkdir`，而这个目录不归任何空间，界面里看不到却真实存在。现在改为在挂载点未解析时**不创建**目录，交给容器入口在共享卷里建

## v1.1.1 — 2026-09-19

### 新增

- `SHARE_ROOT` 安装参数：收窄共享进容器的宿主目录，默认从整个 `/volume1` 改为 `/volume1/data`，可进一步收窄到个人空间，避免容器读到 AList 的凭据库
- `MOUNT_UID` / `MOUNT_GID` 安装参数：指定挂载出来的文件属主，解决 SMB 共享下文件被当成只读的问题

### 变更

- 共享挂载由整个 `/volume1` 收窄为「挂载点所在的那棵子树」

## v1.1.0 — 2026-09-19

### 变更

- 编排模板渲染健壮性：参数留空 / 含符号（`:` `#` `@`）/ 含中文，渲染结果都是合法 YAML，安装参数原样落地且带引号
- 连接串以冒号收尾（rclone 语法必需，漏掉会报 `unquoted config value must end with , or :`）

## v1.0.0 — 2026-09-18

### 新增

- 首个版本：把 AList 的 WebDAV 通过 rclone + FUSE 挂成 NAS 上的真实目录，支持应用中心安装参数（WebDAV 地址 / 账号 / 口令）
- 容器 `restart: unless-stopped`；挂载点 `:rshared` 传播回宿主机
- 死挂载点判据走 `/proc/mounts`（`mountpoint`、`[ -e ]`、`stat` 在死挂载点上全都探测不出来）
